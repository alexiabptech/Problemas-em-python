string =  'banana nanica'
str_nova = string[0:2]
print(str_nova)

#def conta_bigramas(string):
    #retorna um dic com o numero de ocorrencias de cada par de caractere
    # percorrer a string
    # fatiar em 2
    # adicionar cada fatiamento e contar quantas vezes esse mesmo fatiamento ocorrre

    # dic_bi = {}

    # for i in range(len(string)):
    #     bigrama = string[i:i+2]

    #     if len(bigrama) > 1:
                
    #         if bigrama not in dic_bi:
    #             dic_bi[bigrama] = 1
    #         else:
    #             dic_bi[bigrama] += 1
                
    # return dic_bi

